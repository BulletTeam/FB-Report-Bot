{
  "name": "FB Report Bot",
  "version": "1.0.0",
  "description": "Facebook automation / cookie index server (Playwright-ready)",
  "main": "server.js",
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon --watch . --exec \"node server.js\"",
    "lint": "echo \"no lint configured\"",
    "postinstall": "npx playwright install --with-deps || true"
  },
  "author": "Pravas Bera",
  "license": "MIT",
  "dependencies": {
    "body-parser": "^1.20.2",
    "cors": "^2.8.5",
    "express": "^4.18.2",
    "multer": "^1.4.5-lts.1",
    "playwright": "^1.40.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
